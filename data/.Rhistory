# and remove these from the raster stack? and then project these values into a new pancake?
}
y <- lapply(dv1, PeTbyCell)
View(y)
y[["1"]]
pen
str(pen)
class(pen)
names(pen)
names(pen) <- wichita$YEAR
pen
names(pen)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = F)
names(y) <- x$year
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = F)
names(y) <- x$year
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
y <- lapply(dv1, PeTbyCell)
y <- lapply(out, PeTbyCell)
View(y)
out <- drought_vars %>%
rowwise() %>%
split(., .$CellID) %>%
map(.x, PeTbyCell)
out <- drought_vars %>%
rowwise() %>%
split(., .$CellID) %>%
map(PeTbyCell(.x))
out <- drought_vars %>%
rowwise() %>%
split(., .$CellID) %>%
map(PeTbyCell(x))
out <- drought_vars %>%
rowwise() %>%
split(., .$CellID) %>%
lapply(., PeTbyCell)
View(out)
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell)
exit()
knitr::opts_chunk$set(echo = F)
knitr::opts_chunk$set(dpi = 300)
knitr::opts_chunk$set(message = F)
knitr::opts_chunk$set(warning = F)
out_p <- '/media/sagesteppe/ExternalHD/drought_geospatial/monthly_gridmet'
cloudCover <- rast('../data/raw/clouds.tif')
sunHours <- rast('/media/sagesteppe/ExternalHD/UFO_drought/data/raw/MeanSunHours.tif')
dem <- rast('../data/raw/dem.tif')
library(tidyverse)
library(terra)
source('functions.R')
dem <- rast('../data/raw/dem.tif')
vs <- rast(file.path(out_p, 'vs', (list.files(file.path(out_p, 'vs')))))
tmax <- rast(file.path(out_p, 'tmmx', (list.files(file.path(out_p, 'tmmx')))))
tmin <- rast(file.path(out_p, 'tmmn', (list.files(file.path(out_p, 'tmmn')))))
pr <- rast(file.path(out_p, 'pr', (list.files(file.path(out_p, 'pr')))))
names(vs) <- namer(vs, start = 1979)
names(tmax) <- namer(tmax, start = 1979)
names(tmin) <- namer(tmin, start = 1979)
names(pr) <- namer(pr, start = 1979)
names(dem) <- 'elevation'
variables <- c(vs, tmax, tmin, pr, dem)
names(cloudCover) <- paste0('cloudcover_', names(cloudCover))
cloudCover <- cloudCover/100
names(sunHours) <- paste0('sunhours_', names(sunHours))
suncloud <- c(cloudCover, sunHours)
names(cloudCover) <- paste0('cloudcover_', names(cloudCover))
cloudCover <- rast('../data/raw/clouds.tif')
names(cloudCover) <- paste0('cloudcover_', names(cloudCover))
cloudCover <- cloudCover/100
names(sunHours) <- paste0('sunhours_', names(sunHours))
suncloud <- c(cloudCover, sunHours)
sunHours <- rast('/media/sagesteppe/ExternalHD/UFO_drought/data/raw/MeanSunHours.tif')
names(sunHours) <- paste0('sunhours_', names(sunHours))
suncloud <- c(cloudCover, sunHours)
cloudvals <- as.data.frame(suncloud) %>%
rownames_to_column('CellID') %>%
pivot_longer(!CellID, names_to = 'combo', values_to = 'value') %>%
separate(combo, c('variable', 'month')) %>%
pivot_wider(names_from = variable, values_from = value)
drought_vars <- as.data.frame(variables, xy = T) %>%
rownames_to_column('CellID') %>%
pivot_longer(4:(ncol(.)-1)) %>%
separate(name, c('variable', 'month', 'year')) %>%
pivot_wider( names_from = variable, values_from = value) %>%
left_join(., cloudvals)
drought_vars <- as.data.frame(variables, xy = T) %>%
rownames_to_column('CellID') %>%
pivot_longer(4:(ncol(.)-1)) %>%
separate(name, c('variable', 'month', 'year')) %>%
pivot_wider( names_from = variable, values_from = value) %>%
left_join(., cloudvals)
rm(cloudvals, suncloud, vs, tmax, tmin, variables, dem, pr, sunHours)
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell)
View(out)
head(out)
library(SPEI)
data(wichita)
pen <- penman(wichita$TMIN,
wichita$TMAX,
wichita$AWND,
tsun=wichita$TSUN,lat=37.6475, z=402.6, na.rm=TRUE)
pen
View(out)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame(y) %>%
mutate(year = x$year, precip = x$pr,
balance = precip - ETO_pen)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell)
head(out)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame(ETO = y) %>%
mutate(year = x$year, precip = x$pr,
balance = precip - ETO_pen)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame(ETO = y) %>%
mutate(year = x$year, precip = x$pr,
balance = precip - ETO)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame( y) #%>%
#  mutate(year = x$year, precip = x$pr,
#         balance = precip - ETO)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
head(out)
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell)
head(out)
?data.frame
}
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame( y) %>%
rename(ETO = 1) %>%
mutate(year = x$year, precip = x$pr,
balance = precip - ETO)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell)
head(out)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame( y) %>%
rename(ETO = 1) %>%
mutate(year = x$year, precip = x$pr,
balance = precip - ETO)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
View(out)
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell) %>%
bind_rows()
View(drought_vars)
y <- data.frame( y) %>%
rename(ETO = 1) %>%
mutate(year = x$year, precip = x$pr, month = x$month ,
balance = precip - ETO)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame( y) %>%
rename(ETO = 1) %>%
mutate(year = x$year, precip = x$pr, month = x$month ,
balance = precip - ETO)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell) %>%
bind_rows()
head(out)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame( y) %>%
rename(ETO = 1) %>%
mutate(CellID = x$CellID, year = x$year, month = x$month, precip = x$pr,
balance = precip - ETO)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell) %>%
bind_rows() %>%
select(year, month, precip, ETO, balance)
head(out)
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell) %>%
bind_rows() %>%
select(CellID, year, month, precip, ETO, balance)
head(out)
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell) %>%
bind_rows() %>%
select(CellID, year, month, precip, ETO, balance) %>%
arrange(year, month, CellID)
head(out)
PeTbyCell <- function(x, rast){
y <- SPEI::penman(Tmin = x$tmin, Tmax = x$tmax,
U2 = x$vs, lat = x$y[1], #
Rs = NA, Ra = NA, # but could get  both from r.sun ??
tsun = x$sunhours, # CC = drought_vars$cloudcover,
z = x$elevation[1], na.rm = T)
y <- data.frame( y) %>%
rename(ETO = 1) %>%
mutate(CellID = as.numeric(x$CellID), year = as.numeric(x$year), month = x$month, precip = x$pr,
balance = precip - ETO) %>%
select(CellID, year, month, precip, ETO, balance)
return(y)
# here need to determine WHICH cells were not in the complete cases !=,
# and remove these from the raster stack? and then project these values into a new pancake?
}
out <- drought_vars %>%
split(., .$CellID) %>%
lapply(., PeTbyCell) %>%
bind_rows() %>%
arrange(year, month, CellID)
head(out)
gc()
0.85+5.1+1.7
493+475
332/500
56/500
64.4 + 11.2
knitr::opts_chunk$set(echo = TRUE, warning = FALSE)
knitr::include_graphics("../graphics/plots/phenology_estimates_10_90.png")
#set_here(path='.')
setwd('/hdd/Assorted_R_functions/Augspurger_Synchrony/scripts')
pena_flrs <- read.csv("../data/dummy_data.csv")
#set_here(path='.')
setwd('~/Documents/Augspurger_Synchrony/data')
pena_flrs <- read.csv("../data/dummy_data.csv")
head(pena_flrs)
pena_synchrony <- augs_synchrony(dataset = pena_flrs,
frst_day = flower_start, lst_day = flower_end,
year_samp = year, species )
head(pena_synchrony)
#' @keywords phenology, synchrony, ecology, population biology
#'
#' @examples
#' head(pena_flrs)
#' pena_synchrony <- augs_synchrony(dataset = pena_flrs,
#'                                 frst_day = flower_start, lst_day = flower_end,
#'                                 year_samp = year, species )
#' head(pena_synchrony)
#'
#' @export
augs_synchrony <- function(dataset, frst_day, lst_day, year_samp, ...){
frst_day <- rlang::enquo(frst_day)
lst_day <- rlang::enquo(lst_day)
year_samp <- rlang::enquo(year_samp)
grp_vars <- rlang::enquos(...)
dataset <- dataset |>
dplyr::mutate(across(c(!!frst_day, !!lst_day), function(x) as.integer(x)))
results <- dataset |>
dplyr::group_by(!!year_samp, !!!(grp_vars)) |>
# TERM A FOR THE INDIVIDUAL $X_{i}$
# term one population (\frac{1}{n-1})
dplyr::mutate(at1 = 1/(dplyr::n() -1)) |>
dplyr::mutate(dplyr::across(c(!!frst_day, !!lst_day),
function(x)
lubridate::as_date(x, origin =
paste0(!!year_samp - 1, "-12-31")))
) |>
dplyr::mutate(interval_obs = lubridate::interval(lubridate::ymd(!!frst_day),
lubridate::ymd(!!lst_day))) |>
# term two, (\frac{1}{f_{i}}) duration of event for individual.
# note duration returns seconds, but these data are at resolution of day.
dplyr::mutate(at2 = 1/
(as.numeric(
lubridate::as.duration(!!lst_day - !!frst_day)
)
/86400
)
) |>
# term 3: all days individual i is flowering with other individuals
# \sum_{j = i}^{n} e_{j != i}
dplyr::mutate(at3 = purrr::map_dbl(interval_obs, # @ shs on SO
\(x) x |>
lubridate::intersect(interval_obs) |>
lubridate::int_length() |>
sum(na.rm = T) - lubridate::int_length(x)
) / 86400) |>
# the index of synchrony for individual i
dplyr::rowwise() |>
dplyr::mutate(augs.indx.indiv. = at1 * at2 * at3) |>
dplyr::group_by(!!year_samp,  !!!(grp_vars)) |>
## TERM B FOR THE POPULATION
dplyr::mutate(bt1 = 1/dplyr::n()) |>  # $\frac{1}{n}$ 1/sample size
dplyr::mutate(bt2 = sum(augs.indx.indiv.)) |>  # \sum_{j = 1}^{n}X_{i}
dplyr::mutate(augs.index.pop = bt1 * bt2) |>
# clean return data
dplyr::select(-at1, -at2, -at3, -bt1, -bt2, -interval_obs)
return(results)
}
pena_synchrony <- augs_synchrony(dataset = pena_flrs,
frst_day = flower_start, lst_day = flower_end,
year_samp = year, species )
head(pena_synchrony)
head(pena_flrs)
test_dummyp <- test_dummy |>
dplyr::group_by(species) |>
dplyr::mutate(ID = 1:dplyr::n(),
flower_start_DOY = lubridate::yday(flower_start),
flower_end_DOY   = lubridate::yday(flower_end))
test_dummyp <- pena_synchrony |>
dplyr::group_by(species) |>
dplyr::mutate(ID = 1:dplyr::n(),
flower_start_DOY = lubridate::yday(flower_start),
flower_end_DOY   = lubridate::yday(flower_end))
ggplot(test_dummyp, aes(y = ID, x = median_flowers)) +
geom_point(aes(fill = 'Median')) +
facet_wrap('species', scales = 'free', nrow = 1) +
theme_bw() +
geom_segment(aes(x = flower_start_DOY, y = ID, xend = flower_end_DOY, yend = ID,
linetype = 'Range')) +
geom_segment(aes(x = lower_sd, y = ID, xend = upper_sd, yend = ID, linetype = 'mean SD'),
arrow = arrow(length = unit(0.03, "npc"), ends = "both")) +
theme(strip.background = element_blank(),
axis.line = element_line(colour = "black"),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
axis.ticks.y = element_blank(),
axis.text.y = element_blank(),
panel.border = element_blank(),
panel.background = element_blank()) +
labs(title = 'Fictitious Floral Synchonry', x = 'Day of Year', y = 'Individual',
fill = NULL) +
scale_linetype_manual("Durations",
values=c("Range"= 3, 'mean SD' = 1)) +
scale_color_manual(values = c("Median" = 'black'))
library(ggplot2)
ggplot(test_dummyp, aes(y = ID, x = median_flowers)) +
geom_point(aes(fill = 'Median')) +
facet_wrap('species', scales = 'free', nrow = 1) +
theme_bw() +
geom_segment(aes(x = flower_start_DOY, y = ID, xend = flower_end_DOY, yend = ID,
linetype = 'Range')) +
geom_segment(aes(x = lower_sd, y = ID, xend = upper_sd, yend = ID, linetype = 'mean SD'),
arrow = arrow(length = unit(0.03, "npc"), ends = "both")) +
theme(strip.background = element_blank(),
axis.line = element_line(colour = "black"),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
axis.ticks.y = element_blank(),
axis.text.y = element_blank(),
panel.border = element_blank(),
panel.background = element_blank()) +
labs(title = 'Fictitious Floral Synchonry', x = 'Day of Year', y = 'Individual',
fill = NULL) +
scale_linetype_manual("Durations",
values=c("Range"= 3, 'mean SD' = 1)) +
scale_color_manual(values = c("Median" = 'black'))
??ggsave
ggsave(path = '../data/AugsPanel.png')
ggplot(test_dummyp, aes(y = ID, x = median_flowers)) +
geom_point(aes(fill = 'Median')) +
facet_wrap('species', scales = 'free', nrow = 1) +
theme_bw() +
geom_segment(aes(x = flower_start_DOY, y = ID, xend = flower_end_DOY, yend = ID,
linetype = 'Range')) +
geom_segment(aes(x = lower_sd, y = ID, xend = upper_sd, yend = ID, linetype = 'mean SD'),
arrow = arrow(length = unit(0.03, "npc"), ends = "both")) +
theme(strip.background = element_blank(),
axis.line = element_line(colour = "black"),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
axis.ticks.y = element_blank(),
axis.text.y = element_blank(),
panel.border = element_blank(),
panel.background = element_blank()) +
labs(title = 'Fictitious Floral Synchonry', x = 'Day of Year', y = 'Individual',
fill = NULL) +
scale_linetype_manual("Durations",
values=c("Range"= 3, 'mean SD' = 1)) +
scale_color_manual(values = c("Median" = 'black'))
ggsave(path = '../data/AugsPanel.png')
ggsave(path = '../data', filename = 'AugsPanel.png')
ggsave(path = '../data', filename = 'AugsPanel.png', height = 5, width = 5)
ggsave(path = '../data', filename = 'AugsPanel.png', height = 5, width = 5, units = 'cm')
ggsave(path = '../data', filename = 'AugsPanel.png', height = 5, width = 5, units = 'in')
ggplot(test_dummyp, aes(y = ID, x = median_flowers)) +
geom_point(aes(fill = 'Median')) +
facet_wrap('species', scales = 'free', nrow = 1) +
theme_bw() +
geom_segment(aes(x = flower_start_DOY, y = ID, xend = flower_end_DOY, yend = ID,
linetype = 'Range')) +
geom_segment(aes(x = lower_sd, y = ID, xend = upper_sd, yend = ID, linetype = 'mean SD'),
arrow = arrow(length = unit(0.03, "npc"), ends = "both")) +
theme(strip.background = element_blank(),
axis.line = element_line(colour = "black"),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
axis.ticks.y = element_blank(),
axis.text.y = element_blank(),
panel.border = element_blank(),
axis.text.x = element_text(angle = 45)
panel.background = element_blank()) +
ggsave(path = '../data', filename = 'AugsPanel.png', height = 5, width = 5, units = 'in')
ggplot(test_dummyp, aes(y = ID, x = median_flowers)) +
geom_point(aes(fill = 'Median')) +
facet_wrap('species', scales = 'free', nrow = 1) +
theme_bw() +
geom_segment(aes(x = flower_start_DOY, y = ID, xend = flower_end_DOY, yend = ID,
linetype = 'Range')) +
geom_segment(aes(x = lower_sd, y = ID, xend = upper_sd, yend = ID, linetype = 'mean SD'),
arrow = arrow(length = unit(0.03, "npc"), ends = "both")) +
theme(strip.background = element_blank(),
axis.line = element_line(colour = "black"),
panel.grid.major = element_blank(),
panel.grid.minor = element_blank(),
axis.ticks.y = element_blank(),
axis.text.y = element_blank(),
panel.border = element_blank(),
axis.text.x = element_text(angle = 45),
panel.background = element_blank()) +
labs(title = 'Fictitious Floral Synchonry', x = 'Day of Year', y = 'Individual',
fill = NULL) +
scale_linetype_manual("Durations",
values=c("Range"= 3, 'mean SD' = 1)) +
scale_color_manual(values = c("Median" = 'black'))
ggsave(path = '../data', filename = 'AugsPanel.png', height = 5, width = 5, units = 'in')
